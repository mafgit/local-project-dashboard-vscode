{
  "name": "localhub",
  "displayName": "LocalHub: Project Explorer",
  "keywords": [
    "project",
    "explorer",
    "viewer",
    "opener",
    "localhub"
  ],
  "description": "View all projects in one place & open any project in a new tab without having to open it from explorer",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/mafgit/localhub-vscode"
  },
  "engines": {
    "vscode": "^1.104.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "activationEvents": [
    "onView:sidebarView"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "localhub.openProject",
        "title": "Open A Project In New Window"
      },
      {
        "command": "localhub.refreshProjects",
        "title": "Refresh Projects"
      },
      {
        "command": "localhub.showProjectsPanel",
        "title": "Show Projects"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "sidebarViewContainer",
          "title": "LocalHub: Project Explorer",
          "icon": "media/icon.png"
        }
      ]
    },
    "views": {
      "sidebarViewContainer": [
        {
          "type": "webview",
          "id": "sidebarView",
          "name": "LocalHub: Project Explorer",
          "icon": "media/icon.png"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.104.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@typescript-eslint/eslint-plugin": "^8.42.0",
    "@typescript-eslint/parser": "^8.42.0",
    "eslint": "^9.34.0",
    "typescript": "^5.9.2",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
